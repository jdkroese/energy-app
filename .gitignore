# Secrets — never commit
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# Source material (photos of serials/passwords, energy bill, tokens) — local only
docs/inbox/

# Node
node_modules/
dist/
build/
.next/
coverage/

# Local state store (JSON persistence) — runtime data, not source
.data/

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Claude Code — local machine settings (permissions, paths) + per-agent worktrees
.claude/settings.local.json
.claude/worktrees/
