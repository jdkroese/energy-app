**Switch** — boolean toggle for settings and automation rules; solar-green track means on.

```jsx
<Switch label="Battery-first charging" defaultChecked />
<Switch checked={enabled} onChange={(e) => setEnabled(e.target.checked)} />
```
