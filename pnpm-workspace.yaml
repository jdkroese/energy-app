packages:
  - apps/*

# pnpm 11 build-script gate. esbuild ships its binary via an optional dependency,
# so running its install script is safe. (This is the key pnpm itself honors.)
allowBuilds:
  esbuild: true
